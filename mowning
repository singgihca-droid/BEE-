<!DOCTYPE html>
<html lang="id">
<head>
<meta charset="UTF-8">
<title>Untuk Kamu ☁️</title>

<style>
  body {
    margin: 0;
    height: 100vh;
    font-family: 'Segoe UI', sans-serif;
    overflow: hidden;
    background: linear-gradient(to bottom, #87ceeb, #dff6ff);
    display: flex;
    justify-content: center;
    align-items: center;
  }

  /* Awan */
  .cloud {
    position: absolute;
    background: white;
    border-radius: 50px;
    opacity: 0.8;
    animation: moveClouds linear infinite;
  }

  .cloud::before,
  .cloud::after {
    content: '';
    position: absolute;
    background: white;
    border-radius: 50%;
  }

  .cloud::before {
    width: 60px;
    height: 60px;
    top: -30px;
    left: 10px;
  }

  .cloud::after {
    width: 80px;
    height: 80px;
    top: -40px;
    right: 15px;
  }

  @keyframes moveClouds {
    from { transform: translateX(-200px); }
    to { transform: translateX(120vw); }
  }

  .card {
    background: rgba(255,255,255,0.9);
    padding: 30px;
    border-radius: 20px;
    box-shadow: 0 10px 30px rgba(0,0,0,0.15);
    max-width: 420px;
    text-align: center;
    z-index: 10;
  }

  h1 {
    color: #ff5c8a;
  }

  p {
    font-size: 18px;
    opacity: 0;
    transition: opacity 1s;
    margin: 12px 0;
  }

  p.show {
    opacity: 1;
  }

  button {
    background: #ff5c8a;
    color: white;
    border: none;
    padding: 12px 24px;
    border-radius: 30px;
    font-size: 16px;
    cursor: pointer;
    margin-top: 15px;
  }

  button:hover {
    background: #ff336d;
  }
</style>
</head>

<body>

<!-- Awan -->
<div class="cloud" style="width:140px;height:50px;top:15%;animation-duration:60s;"></div>
<div class="cloud" style="width:180px;height:60px;top:35%;animation-duration:80s;"></div>
<div class="cloud" style="width:120px;height:45px;top:60%;animation-duration:70s;"></div>

<div class="card">
  <h1 id="greeting"></h1>
  <p id="question" class="show">siapkah menghadapi hari ini?</p>
  <button id="readyBtn">aku siap!</button>

  <div id="messages">
    <p>hai, bidadari... semoga hari ini cerah dan lebih baik ya</p>
    <p>maaf karena sering mengganggu waktumu :(</p>
    <p>apa lagi hanya untuk urusan rindu</p>
    <p>abis kamu tu bagi aku kek bunyi kereta api</p>
    <p><b>cute.. cute.. cuteeee 💙</b></p>
  </div>

  <audio id="song" src="peak-of-love.mp3" loop></audio>
</div>

<script>
  // Greeting sesuai waktu
  const greeting = document.getElementById("greeting");
  const hour = new Date().getHours();

  if (hour >= 5 && hour < 11) greeting.innerText = "Selamat pagi ☀️";
  else if (hour >= 11 && hour < 15) greeting.innerText = "Selamat siang 🌤️";
  else if (hour >= 15 && hour < 18) greeting.innerText = "Selamat sore 🌇";
  else greeting.innerText = "Selamat malam 🌙";

  const btn = document.getElementById("readyBtn");
  const song = document.getElementById("song");
  const messages = document.querySelectorAll("#messages p");

  btn.addEventListener("click", () => {
    btn.style.display = "none";
    document.getElementById("question").style.display = "none";
    song.play();

    messages.forEach((msg, index) => {
      setTimeout(() => {
        msg.classList.add("show");
      }, index * 1800);
    });
  });
</script>

</body>
</html>
